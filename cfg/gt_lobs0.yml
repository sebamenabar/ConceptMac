GPU_ID: '0'
WORKERS: 1
CUDA: True
LOGDIR: 'gt0'

TRAIN:
    FLAG: True
    LEARNING_RATE: 0.0004
    BATCH_SIZE: 256
    MAX_EPOCHS: 25
    SNAPSHOT_INTERVAL: 5
    WEIGHT_INIT: "xavier_uniform"
    EALRY_STOPPING: True
    PATIENCE: 5
    CLIP_GRADS: True
    RADAM: False
    # MAX_STEPS: 4

DATASET:
    DATA_DIR: '/storage1/datasets/CLEVR_v1.0/features'
    SCENES_DIR: '/storage1/datasets/CLEVR_v1.0/scenes'

model:
    num_lobs: 0
    max_step: 4
    separate_syntax_semantics: False
    common:
        module_dim: 256
    input_unit:
        wordvec_dim: 256
        rnn_dim: 256
        bidirectional: True
        separate_syntax_semantics_embeddings: False
    # control_unit:
    # read_unit:
    write_unit:
        rtom: True
    # output_unit: 
